# 仕様書（SPEC）

## 制作方針

WhiteSpaceは将来のPCゲーム、ARG、実験的サービスを育てるための試作環境。スピード・柔軟性・拡張性・小さな実験を優先する。現行の「宥めよ」に固定しない。明示的な実装依頼では通常の仕様・UI・返答・バランスをエージェントの裁量で決め、理由と検証結果を記録する。詳細は [AGENTS.md](AGENTS.md)。

## 現在のゲーム

対象は `prototypes/001-nadameyo`（Vite + React）。5回の発言で相手の信頼を得る。初期値は信頼0、緊張2、発言数5。

| 評価・行動 | 信頼 | 緊張 | 発言数 |
| --- | ---: | ---: | ---: |
| 謝罪・傾聴・安心 | +1 | 0 | -1 |
| 命令 | 0 | +1 | -1 |
| 拒絶・敵意 | 0 | +2 | -1 |
| 未分類・判断保留 | 0 | 0 | -1 |
| 同じ肯定的な文を再送 | 0 | 0 | -1 |
| 緊張を上げた発言の直後に、初めて使う謝罪文 | +1 | -1 | -1 |

反復は、この会話中の全発言についてNFKC・小文字化・空白/句読点除去後に比較する。言い換えた同義文までは同じ文とみなさない。反復した命令・敵意等の緊張増加は継続する。

空白だけの入力と終了後の入力は無視する。入力欄は最大80文字。成功・失敗判定は「緊張5以上→失敗」「信頼4以上→成功」「発言数0→失敗」の順。同時に最後の発言で信頼4なら成功。数値は信頼0〜4、緊張0〜5に収める。再挑戦は履歴を含め全状態を初期化する。

未分類・判断保留では自然に聞き返し、最終発言では会話終了の返答を優先する。理解できなかった入力をプレイヤーの悪意とみなさない。

## 判定の対応範囲

88件採用・43件保留・13件除外・未レビュー0件。全144件の採否理由と外部辞書の出典は保持する。「話してください」は傾聴。

- 採用表現を正規化した部分一致で照合する。同じ位置を覆う長い採用表現は短い表現より優先する。
- 保留・未レビュー表現が残れば判断保留。「面倒くさい」「きっと大丈夫」を短い採用語だけで断定しない。
- 除外候補自体は照合に使わない。保留語より長い採用表現「話せる範囲でいい」は傾聴になる。
- 肯定カテゴリだけが混在する場合は傾聴→謝罪→安心の順で代表を1つ選び、信頼加点は最大1。肯定と敵意・拒絶・命令などが混在すれば保留する。
- 正規化後2文字以下の保留・未レビュー語は、空白・句読点で区切った区間全体との一致だけに限定する。「ばかり」「バカンス」内の短語では保留しない。「ばか」「ごめん、ばか」は保留。形態素解析ではないため、区切りなしの「ごめんばか」などは検出対象外。長い保留語は従来の部分一致を維持。
- 限定した否定形：「大丈夫じゃ/では/でもない」「大丈夫じゃ/では/でもありません」「大丈夫とは思わない/言えない」「ごめんとは思わない/と思ってない」等を保留。正規表現の具体的な範囲は `intentMatcher.js` に集約する。
- 「一人じゃない」「無理しなくていい」「話さなくてもいい」「責めないよ」は安心。「ない」だけでは除外しない。
- 「知らないわけじゃない」等の二重否定、「面倒を見たい」、「お前の話」、かぎ括弧の直後に「と言/って言/と聞/と書…」などが続く伝聞を限定的に保留する。

一般的な日本語の構文・否定範囲・主語・皮肉は解析しない。例えば「大丈夫なわけがない」「ごめんと言うつもりはない」、かぎ括弧のない伝聞等は未対応で誤判定し得る。長い保留語の部分一致も保守的すぎる場合がある。この小実装で日本語全般を理解できるとは扱わない。

## 処理の境界

`createIntentEvaluator(dictionary)` が返す関数に `{ input, history, state }` を渡す。historyは直近最大5件、stateは信頼・緊張・残り発言数。現行の辞書評価器は入力だけを利用する。

評価結果は `{ intent, disposition, normalizedInput, matches, reason, engine }`。dispositionは `matched` / `uncertain` / `unknown`。状態変化量と返答は含めない。

- `src/lib/intentMatcher.js`: 言語評価。
- `src/lib/gameEngine.js`: 評価器呼び出し、状態更新、反復と歩み寄り、終了判定。
- `src/lib/responses.js`: 状態と直前の行動に応じた返答・変化説明・振り返り。
- `src/App.jsx`: React表示・入力・フォーカス。

ゲームは同期の辞書評価器を使う。比較ラボは独立した非同期モデルアダプターを持ち、ゲームの状態更新には接続しない。将来ゲームへ採用する際は、履歴・状態のモデルへの渡し方とゲーム中の非同期制御を別途検討する。

## 言語比較ラボ（2026-09-06）

`#compare` で同一入力の辞書・ローカルLLM評価を並べる。辞書は既存評価器をそのまま使い、履歴は空、状態は初期値。モデルも同じ入力だけを受け取る。比較中にゲームの数値を変えない。画面切替は入力・結果・ゲーム状態を初期化する。

レビュー例3件と生成例25件の計28件を用意する。暫定期待は正解保証ではなく、モデルへ送らない。自由入力は280文字、Enterは改行。カテゴリ・保留状態・一致表現/モデルの根拠・理由・処理時間・読み込みと推論の内訳を表示する。辞書はブラウザ内時間、モデルはローカルAPIの処理時間で測定範囲が異なる。

モデルは `qwen3.5:2b-q4_K_M`、Ollamaは127.0.0.1:11435、Viteの同一オリジンAPI経由。クラウド推論は使わない。モデル応答の4項目と型・カテゴリ/保留状態の整合・入力内に実在する根拠を検証し、不正なら判定に採用せず診断表示する。形式が正しくても意味が正しいとは限らない。

一括推論は逐次実行。辞書結果は先に全件表示。120秒タイムアウト、待機・中止・接続失敗・形式エラー表示を設ける。形式エラーは次へ進み、接続失敗/タイムアウト/中止は後続を未実行にする。モデル停止中も辞書のみ利用可能。JSON保存は明示ボタン、画面内結果は永続保存しない。CLI実測は別途JSONに記録する。純粋な静的配信では比較APIがなく、モデル利用にはVite dev/previewまたは同等のサーバーが必要。

## 表示と小さな実験

目標値・発言コスト・直前の実際の数値変化を表示。信頼メーターは4区画、緊張は5区画。履歴は最新から順に、発言・返答・変化を表示する。相手が入力されていない言葉を引用する固定返答を廃止し、信頼・緊張・直前の謝罪・反復に反応する。

追加実験は「会話のふり返り」。実際の履歴から届いた言葉・聞き返し・反復・歩み寄りを数え、結果に合わせて次の試し方を案内する。保存・送信はしない。開発時だけ折りたたみの判定内訳を表示し、一致表現・保留理由・反復を確認できる。
