# プロジェクト状態（PROJECT STATE）

最終更新: 2026-09-06

## 最新の区切り：辞書とローカルLLMを比較できる一版

前回改善版 `ab184da` と最新リモートを確認し、未コミット差分なしから `codex/local-language-comparison` を作成。作業場所は引き続き下記worktree。mainは旧版のため使わない。

保存済み: `f8ca872`（辞書修正）、`1d78a26`（比較ラボ・実測・文書）。既存認証でGitHubの同名作業ブランチへpush成功。この保存結果の追記も同ブランチの記録コミットで管理する。

3つのレビュー指摘を回帰テスト付きで修正。辞書144候補の採否・理由・出典は変更なし。`#compare` に独立した言語比較ラボを追加し、28例（レビュー3＋生成25）を辞書とQwen3.5で実測した。保留・エラー・時間内訳・差分を観察できる。

PCはRyzen 7 5700X / RAM32GB / RTX4070 SUPER 12GB。Ollama v0.33.3、モデル `qwen3.5:2b-q4_K_M`（約1.945GB）をworktreeの無視対象 `.local-llm/` に配置。ローカルCUDA推論のみ。モデルアンロード後の初回4.867秒、読み込み後中央値0.614秒。最初の環境初期化は30秒で2回タイムアウトし、完了時間未測定。

28例中17例は形式が有効、11例は不正。言い換えを拾える一方、肯定混在・無関係な天気の文などを誤解するため、ゲームの判定は辞書を維持。テスト28件・lint・build成功。実ブラウザで比較・エラー・モデル停止・中止/再実行・ゲーム成功/失敗/再挑戦・狭い画面を確認。実IMEは未確認。

[起動とモデル詳細](docs/LOCAL_MODEL.md) / [今回の実操作](docs/PLAYTEST_2026-09-06.md) / [実測JSON](docs/experiments/language-comparison-2026-09-06.json)。次は同じモデルで応答形式とプロンプトを小さく改善し、未使用ケースで評価する。以下は前回までの履歴。

## 前回の区切り（2026-09-05）

WhiteSpaceは自由に変更できる試作環境。スピード・柔軟性・拡張性・小さな実験を優先する。現在のアプリは `prototypes/001-nadameyo` の「宥めよ」（Vite + React）。制作方針と進め方は [AGENTS.md](AGENTS.md)。

今回の区切り「レビュー反映と、実際に遊んで改善を確認できる一版」を実装した。辞書は88採用・43保留・13除外、全144件の理由を保持。「話してください」は傾聴。未分類・保留では数値を変えず聞き返し、毎送信1回を消費する。

否定誤判定の限定修正、同文反復の加点停止、強い言葉の直後の謝罪による緊張回復を追加した。言語評価・ゲーム更新・返答を分離。UIに目標・変化・履歴を表示し、追加実験として終了時の振り返りを設けた。開発時は判定の内訳を確認できる。

## 作業場所とGit

- 今回の実cwd: `C:\Users\masat\.codex\worktrees\2f05\codex_test`
- 元checkout: `C:\Users\masat\Documents\codex_test`。今回の編集対象ではない。
- 開始時: 未コミット差分なし、HEADは `d731a53`（ブランチ未所属）。権限付きfetch後のorigin/mainも同じコミット。
- 作業ブランチ: `codex/intent-review-playable`。mainへのマージは行わない。
- レビュー取込: `c6158cd`。改善実装: `155c07b`。両方をGitHubの同名作業ブランチへpush済み。以降の記録コミットも同ブランチで管理する。

## 確認結果

- 取込直後: テスト10件、lint、build成功。ブラウザ入力・成功・緊張失敗・再挑戦成功。
- 改善版: テスト20件、lint、build成功。採用88表現、否定、曖昧入力、反復、回復、終了判定、評価器境界を検証。
- Codex内ブラウザで改善前後の表示、通常幅/390px指定の狭い画面、成功・2種類の失敗・再挑戦・開発パネルを操作。横はみ出しとコンソール警告/エラーなし。
- 今回のURLは `http://127.0.0.1:5174/`。元の5173と混同を避けるため分離。起動状態は記録時点のもので、将来の稼働を保証しない。
- Windows Node `v24.16.0` / npm `11.13.0` を確認。必要時は `C:\Program Files\nodejs\npm.cmd` を使用。

## 前回時点の残る課題（今回の対応は上記）

一般的な日本語の構文解析は未実装。否定の言い換え・主語・皮肉・保留語の部分一致などで誤判定し得る。正規化後に異なる同義文は反復と扱わない。実IMEの変換確定Enterは未確認。外部辞書の出典は保持し、利用条件の再確認は今回行っていない。

次は自由入力の実例から小さな評価ケースを増やすのが有力。詳細は [TODO](TODO.md)、[仕様](SPEC.md)、[検証記録](docs/PLAYTEST_2026-09-05.md)、[アプリ状態](prototypes/001-nadameyo/APP_STATE.md)。
