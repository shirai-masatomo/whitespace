# APP STATE: 宥めよ

最終更新: 2026-09-10。現在の仕様は [SPEC](../../SPEC.md)、実操作の結果は [検証記録](../../docs/PLAYTEST_2026-09-08.md)。

## コーヒー場面実験

#coffee をWorkspaceへ追加。CoffeeScene.jsxが状態/選択肢/自由入力の確認を管理し、lib/coffeeScene.jsが四つの外殻・表示用既知情報・唯一の状態更新境界を持つ。lib/coffeeLanguage.jsは解釈契約、server/coffeeModel.jsは既存ローカルモデル通信。lib/coffeeCharacter.jsが得点なしで表示値へ変換し、CharacterのdisplayState入力で既存造形を使う。条件切替/再開始で人物と推論も初期化する。

現在は全52テスト。coffeeRules.jsに優先順付きの返答・条件・更新規則を分離。coffeeScene.jsは発話根拠付きの知識更新と履歴を管理する。CoffeeTable.jsxが物の状態から机上を描画し、発話と実動作を別に表示。通常候補/検証操作を分けた。図生成と全到達遷移の検証はscripts/coffeeGraph.mjsを共有する。

[現行仕様・確認](../../docs/COFFEE_PRESENCE_REVIEW.md) / [全体遷移図](../../docs/COFFEE_TRANSITIONS.md)。自由入力は過剰保留が残る実験であり、選択肢を置き換えない。[初版の実測](../../docs/COFFEE_CONTEXT.md)。

## 状態に反応する対話相手

`Character.jsx` はゲーム状態を読み、独立した表示プレビューと代替表示を管理。手動の動き軽減はAppが保持し再挑戦でも維持。Appのroundをkeyにして、再挑戦でCharacterと描画/イベントを初期化する。

- `src/lib/characterState.js`: 純粋な状態→姿勢/説明/発言イベント、同一IDの再生防止、短い反応、補間。
- `src/character/createBust.js`: 頭・首・肩・胸・光のコード生成と表示値の適用。
- `src/character/createCharacterRenderer.js`: Three.js、照明/カメラ、サイズ追従、RAF、非表示時の休止、失敗と破棄。
- `src/character/disposeScene.js`: 共有形状・材質・テクスチャを一度ずつ解放。
- `src/Character.css`: 人物・確認パネル・静止SVGの配置。
- `test/characterState.test.js` / `characterResources.test.js`: 4組合せ、実ゲームイベント、終了優先、重複防止、初期化、補間、動き軽減、共有資源の解放。

胸像版時点で33テスト。Three.js 0.185.1を1依存だけ追加し、3D部分は動的import。外部モデル/画像/LLMを表示条件にしない。WebGL喪失時はループと資源を解放し、SVGで会話継続。プレビューは得点・履歴を書き換えない。

遊び方/履歴を折りたたみ、送信後は人物全体と返答/入力が見える位置へ戻す。確認パネルはPCで人物横、狭い画面で人物下。productionパネル非表示も実操作済み。以下は既存ゲームと比較ラボの構成。

## 言語比較ラボ

`Workspace.jsx` が `#game` と `#compare` を切り替える。切替時は両画面の入力/結果をリセット。ゲームの標準評価は辞書のまま。`Comparison.jsx` は独立した入力・比較行・進捗・接続状態を保持し、辞書の結果を即時表示してからローカルモデルを逐次呼ぶ。中止/接続失敗で未処理行を未実行にし、形式不正は診断を残して次へ進む。

`src/lib/comparison.js` は既存辞書評価器とクライアントAPI、`server/localModel.js` は固定Ollamaへの接続・時間測定・同一オリジン制限、`modelContract.js` はプロンプト・JSON schema・検証を担当。`comparisonCases.js` は生成例とレビュー例を区別する。実測スクリプトは同じ評価実装を呼び、モデル結果を辞書で代用しない。履歴/状態を使ったモデル判定は今回導入しない。

肯定だけの混在は傾聴→謝罪→安心で代表を選び1加点まで。短い保留語は区切り全体一致、丁寧な安心の否定も保留。モデルには誤解と不正形式が多く、ゲームへ直接つなげない。テストはゲーム/辞書22件とモデル/API6件の計28件。

## 状態と遷移

ゲーム状態は `gameEngine.js` の1オブジェクトにまとめる。初期値は tension=2、trust=0、turnsLeft=5、result=playing、endReason=null、history=[]。Reactはこれに入力欄の文字列を持つ。

送信時は空白/終了済みを除外→言語評価器に入力・直近5履歴・数値状態を渡す→反復/歩み寄りを判定→数値更新→発言数を1減らす→緊張5/信頼4/発言切れの順に終了判定→履歴を追記する。履歴には入力、評価結果、反復、実際の数値差分、発言後の状態を保持する。以前の状態は書き換えない。

未分類・判断保留では信頼/緊張は0変化。同文反復の肯定発言は0加点。直前に緊張を上げた発言があり、新しい謝罪文を送ると信頼+1、緊張-1。成功と発言切れが重なれば成功。終了後は入力不可。再挑戦はすべてリセットする。

## ファイルの責務

- `src/data/intent-dictionary.json`: 意味カテゴリと144件の候補・採否・出典。v3で旧固定返答/数値変化を除去し、言語データに限定。
- `src/lib/intentMatcher.js`: 正規化・採用語照合・保留と限定的な誤判定対策。戻り値にゲーム数値や返答を含めない。
- `src/lib/gameEngine.js`: 初期化・ゲーム規則・状態更新。評価器を引数で差し替え可能。
- `src/lib/responses.js`: 最新の行動と状態に応じた返答、数値変化の説明、終了時の振り返り。
- `src/App.jsx`: 入力・表示・開発パネル。IME変換確定Enterの誤送信を防ぐ処理あり（実IMEの操作確認は未実施）。
- `test/intentMatcher.test.js` / `test/gameEngine.test.js`: 言語・進行・返答・境界の22テスト。`test/localModel.test.js` にモデル契約/APIの6テスト。

## 画面

指令と残り発言数、4区画の信頼/5区画の緊張、相手の表情を示す短い状態文、返答、直前の変化、入力欄、遊び方を表示。入力欄は履歴より上に置き、送信後もフォーカスを戻す。終了後は結果理由と振り返り、再挑戦ボタン。履歴は最新を先頭にする。

開発サーバーだけに「開発用：判定の内訳」を表示。一致した表現、正規化した入力、保留の理由、反復/歩み寄りを確認できる。本番ビルドでは非表示。

## 判断理由

- 理解できない言葉でプレイヤーを罰しないため、未分類・保留は中立。発言数は依頼通り消費する。
- 単語追加だけで断定を増やさないため、長い保留語と肯定以外を含むカテゴリ混在は聞き返す。
- 相手の反応を読む動機を作るため、同文反復を加点停止にし、謝罪で緊張を1回復できるようにした。
- 追加実験の振り返りは次の言葉選びに役立ち、モデルや保存機能を増やさずに試せる。
- ゲームの同期評価境界を維持し、非同期モデル制御は独立比較ラボで小さく試す。

## 変更履歴

- 2026-06-01: 最小試作の状態と遷移を記録。意味カテゴリと入力正規化を導入。
- 2026-09-03: 辞書外部化、144候補（20有効/124レビュー）、自動テスト6件。文書日本語化。
- 2026-09-05（別環境）: 124件を68採用・43保留・13除外へレビュー。テスト10件成功、ブラウザ未確認。原記録は [INTENT_REVIEW_OUTCOME](../../docs/INTENT_REVIEW_OUTCOME.md)。
- 2026-09-05（このworktree）: パッチ取込とブラウザ確認。その後、現仕様へ判定/進行/返答/UIを改善。テスト20件と操作確認。詳細は [COMMAND_LOG](../../docs/COMMAND_LOG.md)。
