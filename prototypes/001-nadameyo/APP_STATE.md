# APP STATE: 宥めよ

最終更新: 2026-09-05。現在の仕様は [SPEC](../../SPEC.md)、実操作の結果は [検証記録](../../docs/PLAYTEST_2026-09-05.md)。

## 状態と遷移

ゲーム状態は `gameEngine.js` の1オブジェクトにまとめる。初期値は tension=2、trust=0、turnsLeft=5、result=playing、endReason=null、history=[]。Reactはこれに入力欄の文字列を持つ。

送信時は空白/終了済みを除外→言語評価器に入力・直近5履歴・数値状態を渡す→反復/歩み寄りを判定→数値更新→発言数を1減らす→緊張5/信頼4/発言切れの順に終了判定→履歴を追記する。履歴には入力、評価結果、反復、実際の数値差分、発言後の状態を保持する。以前の状態は書き換えない。

未分類・判断保留では信頼/緊張は0変化。同文反復の肯定発言は0加点。直前に緊張を上げた発言があり、新しい謝罪文を送ると信頼+1、緊張-1。成功と発言切れが重なれば成功。終了後は入力不可。再挑戦はすべてリセットする。

## ファイルの責務

- `src/data/intent-dictionary.json`: 意味カテゴリと144件の候補・採否・出典。v3で旧固定返答/数値変化を除去し、言語データに限定。
- `src/lib/intentMatcher.js`: 正規化・採用語照合・保留と限定的な誤判定対策。戻り値にゲーム数値や返答を含めない。
- `src/lib/gameEngine.js`: 初期化・ゲーム規則・状態更新。評価器を引数で差し替え可能。
- `src/lib/responses.js`: 最新の行動と状態に応じた返答、数値変化の説明、終了時の振り返り。
- `src/App.jsx`: 入力・表示・開発パネル。IME変換確定Enterの誤送信を防ぐ処理あり（実IMEの操作確認は未実施）。
- `test/intentMatcher.test.js` / `test/gameEngine.test.js`: 言語・進行・返答・境界の20テスト。

## 画面

指令と残り発言数、4区画の信頼/5区画の緊張、相手の表情を示す短い状態文、返答、直前の変化、入力欄、遊び方を表示。入力欄は履歴より上に置き、送信後もフォーカスを戻す。終了後は結果理由と振り返り、再挑戦ボタン。履歴は最新を先頭にする。

開発サーバーだけに「開発用：判定の内訳」を表示。一致した表現、正規化した入力、保留の理由、反復/歩み寄りを確認できる。本番ビルドでは非表示。

## 判断理由

- 理解できない言葉でプレイヤーを罰しないため、未分類・保留は中立。発言数は依頼通り消費する。
- 単語追加だけで断定を増やさないため、長い保留語と複数カテゴリは聞き返す。
- 相手の反応を読む動機を作るため、同文反復を加点停止にし、謝罪で緊張を1回復できるようにした。
- 追加実験の振り返りは次の言葉選びに役立ち、モデルや保存機能を増やさずに試せる。
- 複雑な基盤を先行させず、同期の評価境界を1つ設けた。非同期モデル導入時の制御は別作業。

## 変更履歴

- 2026-06-01: 最小試作の状態と遷移を記録。意味カテゴリと入力正規化を導入。
- 2026-09-03: 辞書外部化、144候補（20有効/124レビュー）、自動テスト6件。文書日本語化。
- 2026-09-05（別環境）: 124件を68採用・43保留・13除外へレビュー。テスト10件成功、ブラウザ未確認。原記録は [INTENT_REVIEW_OUTCOME](../../docs/INTENT_REVIEW_OUTCOME.md)。
- 2026-09-05（このworktree）: パッチ取込とブラウザ確認。その後、現仕様へ判定/進行/返答/UIを改善。テスト20件と操作確認。詳細は [COMMAND_LOG](../../docs/COMMAND_LOG.md)。
