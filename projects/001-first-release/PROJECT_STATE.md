# DIVE DIVE — PROJECT STATE

更新: 2026-09-21

**Phase 3 / リアル層 / 品質判定CHANGES / 定期自律開発PAUSED**。現在は面白い空間を速く発見する試作フェーズ。通常は対象操作と1〜3画面、重い検証は節目だけ（AUTONOMY参照）。直近の実装は地形・密度・探索性の3案比較。自動開発再開・人間レビュー待ちへの移行ではない。

## 現在動くもの

- 海上から前へ飛び込むと約32.40mの大岩盤へ着地。幅/高さの違う段丘と沖側の崩れ、海藻帯、崩落群、小石、下面へ抜ける裂け目。左の藻で補給、横を歩く、穴へ潜る、外側へ泳ぐことができる。
- 12案からA地形/B植生と崩落/C岩屋根を実際に試作。切断ブロックに見えるCと、強い斑点模様の材質は棄却。A+Bを修正し、穴を通る別経路を検証。
- 操作/酸素/450mゴールは維持。自然沈降5m/s、E15m/s・酸素2.5倍、Space6m/s、水平7m/s。藻で即100%、救助は同じ世界内。既存11経路と160m以深の地形は維持。
- 出力: `build/windows-agent/DIVE DIVE.exe` / `build/DIVE-DIVE-windows.zip`。[起動方法](README.md) / [比較・連続試走](REVIEW_PACKET.md)。旧ビルド4フォルダは整理済み。

## 指示・検証・未解決

正本はmain 815e26dまで確認。HUMAN_DIRECTIONの地形重点を保持し会話の速度優先方針を追記、EXTERNAL_AI_REVIEWは815e26dへ同期。DD-075の比較は完了。最新レビューの未実装P0（主形状分割、裂け目の予告と見返り、旧アーチ/160m地形）は既存TASKSへ具体化。速度優先の検証方針はAGENTS/AUTONOMYへ反映。

直近実装eee5db8の検証（今回の運用文書変更では再実行しない）: 全check/CI #213成功。 format/lint/依存、479ルール/430シーン/57音/689実衝突/30探索/163岩礁/35入水/30岩盤/179峡谷、2688表面点、11経路、Windows export/配布EXE headless。両rendererで岩盤30検査、通常visual、配布EXE GPU120フレーム。新しい30m視点は実GPU中央値3.01ms/p95 4.51ms、別PCは未測定。

藻→裂け目の縁へ歩行→穴を潜る→外側は8.43秒/救助なし。桟橋からの元の岩盤往復は34.83秒、横断接地229フレーム。どちらも既知の操縦で、初見の面白さを証明しない。描画と同じ三角形から衝突を作り、通路を塞ぐ地形/配置を形側で修正。

未解決: 着地正面の大面/旧アーチには模型感が残り、穴は近づく前には一部隠れる。160m大岩体の連続sweep感、裂け目の先の更なる見返り、潮流の帰還先の読取りも継続課題。HUMAN_REVIEWに戻さずCHANGES。今回の限定実行後も定期開発は保留。

## Git・運用

[下書きPR #2](https://github.com/shirai-masatomo/whitespace/pull/2)、branch `codex/dive-dive-playground`。対象SHAのCIはPR Checksを正本とする。

30分の常設再開`dive-dive`は**PAUSEDのまま**。今回は指定範囲だけ変更・検証・記録し、次タスクの自動実行は人間の再開連絡を待つ。GPU確認は所有する画面外・無音・非捕捉プロセスのみ。人間のアプリ/ゲームを終了しない。

環境メモ: sandbox内GodotはAppData設定にアクセスできないため通常権限で検証。GPUの成否はラッパーExitCode/ログで判定し、古いLASTEXITCODEを使わない。UTF-8文書のPython読み書きはWindows既定cp932を避けてencodingを明示。

## 再開後の候補（自動実行しない）

1. 着地正面の面と裂け目の見え方、旧アーチの単純さを場所単位で磨く。
2. DD-064/069: 左泡/右庭/岩盤の行為差と、裂け目先に魚なしでも成立する見返り。
3. DD-072: 潮へ入る前から退避先を読み取れる形と、岩窓上へ行く理由。
