# DIVE DIVE — PROJECT STATE

更新: 2026-09-21

**Phase 3 / リアル層の場所づくり / CHANGES・SELF_CONTINUE**。人間レビュー待ちではない。Steamなしでゲームを成立させ、最終的にSteam公開可能なWindowsゲームへ進める。

## 現在動くもの

- 海上から入水。自然沈降5m/s、E15m/s・酸素2.5倍、Space6m/s、水平7m/s。酸素25秒、藻で即補給、酸素切れは同じ世界で救助。450m/既存11経路を維持。
- 左の巨大泡/アーチ、右の魚群/藻庭から岩礁の内側/屋根/外側、空気洞の歩行へ。藻庭を既存裂け目寄りに移し、魚群の行先も連動。通常到着で左の水の抜け/対岸と右の高い肩が見える。魚群の疎密/大小も試作したが模型感は未解消。
- 約160mから段丘/狭棚/西の張り出し/東の崩落湾。入り江の既存藻を内岸へ移し、元の西岸の歩行面も保持。80%では内外どちらも補給できるが、70%では内側のみ到達し、外回りは同じ世界で救助となる。
- 入り江→岩窓→24m/sの潮→既存流木の経路。途中で岸へ戻る通し操作は22.45秒/最低15.07%。流木再合流は25.07秒/最低3.40%で、初見の安全路とは呼べない。
- 最新成果物: `build/windows-agent/DIVE DIVE.exe`。[起動方法](README.md) / [画像・測定](REVIEW_PACKET.md)。人間が使用するwindows-preview等は置換しない。

## 最新指示と品質判定

正本はorigin/main **2895a9f** のHUMAN_DIRECTION / EXTERNAL_AI_REVIEW（reviewed_through **440e65a**）。外部レビューは作業ブランチへ同期済みで、DD-064/067/069/072の完了条件へ具体化した。mainの古いTASKS/AUTONOMYで上書きしない。

今回の実装440e65aではDD-064の二段階発見を試作。既存96尾を庭へ向かう72尾と、庭から裂け目へ潜り再び現れる24尾に配分。魚の形/行動を頭数で決めず `OPEN_WATER / ALGAE / FLOW / PASSAGE` の明示roleへ分離し、藻に集まる群れにも適用。左の泡へ寄る自由、地形、移動/O2は維持。

低い巡回は実画面で見えず、GPU全個体確認で28重なり/4横切りも出たため棄却。庭付近で姿を見せてから内部へ潜る進路へ変更し、壁に接触する帰路も修正。24尾×169時点の描画揺れ込み検査と周期境界回帰を追加。97〜99は庭に留まって見る検査視線で、通常84〜88と混同しない。

外部レビュー判定は引き続き **CHANGES / SELF_CONTINUE**。role分離/手掛かり試作は前進だが、84の左右の行為差、裂け目先の見返り、岩/葉の模型感、91の帰還先遮蔽、82の上へ行く報酬は未解決。可遊域が増えた、面白さが合格したとは扱わない。

## 検証

最終check成功: format/lint/依存、ルール479・シーン430・音57・実衝突689・探索30・岩礁157・入水35・峡谷156、補助表面2688点、既存11経路、Windows export/配布EXE headless。

入水35はForward+/Compatibility GPU、入り江76はForward+/Compatibility GPU、峡谷全体152はForward+でも成功。標準visualの救助/再挑戦/到達、配布EXE両描画120フレームも成功。Eで岸側面へ当たる→退避/Space→藻へ実接触、通常/E/Spaceの既存衝突を維持。GPUは画面外/無音/非捕捉。別GPU・初見探索・長時間プレイは未確認。保存/Steam SDK/450m以深は未実装。

最新外部レビュー時点で実装を含むHEAD `3fdb62b` のDIVE DIVE CI run #195はin_progress。失敗した場合は次の制作より先に自律修正する。レビュー/タスク同期commitは実装品質の前進に数えない。

## GitHubと再開

[下書きPR #2](https://github.com/shirai-masatomo/whitespace/pull/2)、branch `codex/dive-dive-playground`。実装reviewed_throughは `440e65a`、最新のレビュー/タスク同期はmain `2895a9f` / 作業枝 `12dd729`。対象SHAのCI結果はPR Checksを正本とする。

30分の常設再開 `dive-dive` は承認済み。再承認不要。過去の無commit時間の原因が解消したとは扱わず、次回も実装commit時刻を確認する。

検査メモ: headlessのMultiMesh getterは描画個体配置を保持しないため、CPU側の同一配置を残して全24尾を検査。GPUでは実インスタンスとの一致も確認。初案はGPUのみ28重なり/4横切りを検出し、修正後は半径1m/169時点で0。微小な負時刻の剰余が配列長へ丸まる境界も回帰検査。

環境メモ: sandbox内GodotはAppData設定/証明書にアクセスできないため、この環境では通常権限で検証。GPUラッパーはStart-ProcessのExitCodeと専用ログで判定。呼び出し元の未設定LASTEXITCODEを非0と比較すると後続検査を省略するため、連続呼び出しはStop例外/ログで判定する。

## 次の3タスク

1. DD-064/067: 魚の追加を止め、通常84で左泡/右庭に別の行為理由を作る。85/86で裂け目/暗がり/別空間が強まり、待機せず気づけること。岩の大面/直線輪郭/葉の規則反復も場所の大形状から再制作。
2. DD-064/069: 裂け目PASSAGEの先に、魚なしでも成立する安全域/歩行/俯瞰/短縮/未知の最低1つを接続。外回り70/80%の危険差も報酬差へ接続する。
3. DD-072: 岩窓上へ行く見返りを追加し、91の隠れた帰還先、流木再合流3.40%のナイフエッジ、粒子以外の流れ/退避域の手掛かりを改善。速度/O2だけの調整で済ませない。
