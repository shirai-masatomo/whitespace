# DIVE DIVE — PROJECT STATE

更新: 2026-09-20

**Phase 3 / リアル層の場所づくり / CHANGES・SELF_CONTINUE**。人間レビュー待ちではない。最終目標はSteam公開可能なWindowsゲーム。現在はSteamなしでゲームを成立させる段階。

## 現在動くもの

- 海上から入水、自然沈降5m/s、E15m/s・酸素2.5倍、Space6m/s。藻で即補給、酸素切れは同じ世界で救助。450mの既存経路。
- 浅海の藻庭から横へ入り縦穴へ抜ける裂け目。縁の格子状の角を除き、内部の壁に窪みを追加。従来の岩礁歩行・空気洞・屋根/外側・泡上昇流も維持。
- 160〜240mは段丘/岩橋/下側の藻庭と、侵食された連続岸壁の内外経路。岩柱の反復を層状の欠けへ置換。125mから連続操作で歩行/遊泳できる。
- 橋からエイ/魚群を追って沖へ回り、岩壁下の別入口から下側藻庭へ入れる試作。満タンは実着地確認込み22.40秒・最低酸素15.20%。80%開始では外回りは救助、早期に内部へ降りる経路は17.05秒・最低16.6%で補給。近景のエイは紙状から曲面へ再制作。
- 通常の開始視点でも水面裏が平らな帯にならず揺らいで見える。左の20m巨大泡と右の藻庭が寄り道先。泡の奥のアーチを浅く奥へ再配置し、穴と支柱を予告。泡→潮流→アーチ→補給は実入力で約29.2秒。
- 最新ビルド出力は `build/windows-agent/DIVE DIVE.exe`。人間の `windows-preview` 等は置換しない。[起動方法](README.md) / [比較と測定](REVIEW_PACKET.md)。

## 検証と同期

最新の外部レビュー正本は `origin/main` の `002c9c1`（`reviewed_through: e7ffe4c`）。作業ブランチへ同内容を同期し、TASKSへ反映済み。HUMAN_DIRECTIONは正本本文と会話補足を分離して保持。最新レビューの未実装指示はDD-064（入水右側の誘い）、DD-067（画面品質ゲート）、DD-069（125〜160m通常経路からの生物ルート自然発見 + 別入口の先の次の好奇心）、DD-070（場所ごとの主役差 + 岩橋大形状）へ具体化。DD-065/068も継続P0。

最終コード `e7ffe4c` で `./dev.ps1 check -BuildFolder windows-agent` 成功。format/lint/依存、ルール479・シーン430・音57・実衝突689・探索30・岩礁/藻庭救助157・入水17・峡谷57、補助の表面2688点、既存11経路、Windows export/EXE headless起動。今回の峡谷/生物の別入口はForward+/Compatibilityで実GPU画面・連続操作を確認、配布EXE Forward+120フレームも成功。生物の連続移動線（中心/左右7m）の壁横切り0。下側補給は側面接触ではなく実着地 + 酸素100%まで要求。入水両方式/標準visual救助・ゴールは前版6d1cf49時点の確認を保持し、今回の景観改善には数えない。GPUは画面外・無音・非捕捉、自分のプロセスのみ終了。

GitHubは[下書きPR #2](https://github.com/shirai-masatomo/whitespace/pull/2)、作業ブランチは `codex/dive-dive-playground`。最新実装 `e7ffe4c` のCI `DIVE DIVE` run #108 は成功。追加の自己監査で壁検査を3秒の実接触へ強化、救助とタイムアウトを区別（57検査成功）。ユーザーの継続的な明示許可を受け、30分ごとの自律再開設定 `dive-dive` をACTIVEで作成済み。同じ範囲の再承認は不要。許可範囲・除外事項はAUTONOMYに記録。ゲームの判定はCHANGESのまま。

## 未解決と次の作業

1. DD-070 / DD-067: 峡谷の左右差は前進したが、区間ごとの「場所の主役差」と岩橋の人工的な中央線が未解決。機能/検証だけが進むサイクルを続けず、通常ルートの前後画面で大形状の品質向上を出す。
2. DD-064: 通常入水で左の泡→アーチは予告できるが、右の藻庭→横穴/縦穴を調べる理由が弱い。通常開始で性質の違う2方向の「何が見える/なぜ行きたい/先で何が変わる」を成立させる。
3. DD-069 / DD-068: 生物→沖→下側入口は実到達可能になった。次は125〜160mの通常経路から自然に発見できるかを確認し、別入口の先に深部予告/観察点/近道など次の好奇心を作る。狭棚/遊泳の選択も通常経路から自然に読める見返りへ繋げる。

環境メモ: sandbox内のGodot importはAppData設定/証明書にアクセスできず失敗する。この環境では通常権限でheadless checkを実行する。

旧レビューPNG70枚と孤立した旧東側アーチを整理。最新証跡・再現コード・履歴・使用中ビルドは保持。別GPU/初見探索/長時間プレイは未確認。保存/Steam SDK/450m以深は未実装。