# DIVE DIVE — PROJECT STATE

更新: 2026-09-21

**Phase 3 / リアル層の場所づくり / CHANGES・SELF_CONTINUE**。人間レビュー待ちではない。Steamなしでゲームを成立させ、最終的にSteam公開可能なWindowsゲームへ進める。

## 現在動くもの

- 海上から入水。自然沈降5m/s、E15m/s・酸素2.5倍、Space6m/s、水平7m/s。酸素25秒、藻で即補給、酸素切れは同じ世界で救助。450m/既存11経路を維持。
- 左の巨大泡/アーチ、右の魚群/藻庭から岩礁の内側/屋根/外側、空気洞の歩行へ。藻庭を既存裂け目寄りに移し、魚群の行先も連動。通常到着で左の水の抜け/対岸と右の高い肩が見える。魚群の疎密/大小も試作したが模型感は未解消。
- 約160mから段丘/狭棚/西の張り出し/東の崩落湾。入り江の既存藻を内岸へ移し、元の西岸の歩行面も保持。80%では内外どちらも補給できるが、70%では内側のみ到達し、外回りは同じ世界で救助となる。
- 入り江→岩窓→24m/sの潮→既存流木の経路。途中で岸へ戻る通し操作は22.45秒/最低15.07%。流木再合流は25.07秒/最低3.40%で、初見の安全路とは呼べない。
- 最新成果物: `build/windows-agent/DIVE DIVE.exe`。[起動方法](README.md) / [画像・測定](REVIEW_PACKET.md)。人間が使用するwindows-preview等は置換しない。

## 最新指示と品質判定

最新外部レビュー正本は origin/main **fb208997**、`reviewed_through: 28ed30e`。作業ブランチの `EXTERNAL_AI_REVIEW.md` へ同内容を **222088d** で同期済み。HUMAN_DIRECTION正本も保持し、未実装指示はTASKSのP0へ具体化済み。

最新レビューは、藻庭の進入角/補給位置変更、魚群の疎密/大小、入り江の内岸補給、岸帰還最低酸素15.07%を前進と評価。ただし通常入水84の差、藻庭85/入り江79の大面・模型感、91の帰還先遮蔽、流木再合流3.40%、魚の同一mesh感は未解決。判定は **CHANGES / SELF_CONTINUE** のまま、人間レビュー条件は未達。

mainのAUTONOMY/TASKSは作業ブランチより古いため上書きしない。正本はHUMAN_DIRECTION / EXTERNAL_AI_REVIEWの2文書。作業ブランチ側の正本fetch、30分再開許可、PC非干渉、同条件画面比較ルールを維持する。

## 検証

最終実装 `28ed30e` のcheck成功: format/lint/依存、ルール479・シーン430・音57・実衝突689・探索30・岩礁157・入水31・峡谷130、補助表面2688点、既存11経路、Windows export/配布EXE headless。

入水31はForward+/Compatibility GPU、入り江50はForward+ GPU、標準visualの救助/再挑戦/到達、配布EXE両描画120フレームも成功。Eで岸側面へ当たる→退避/Space→藻へ実接触、通常/E/Spaceの既存衝突を維持。GPUは画面外/無音/非捕捉。別GPU・初見探索・長時間プレイは未確認。保存/Steam SDK/450m以深は未実装。

## GitHubと再開

[下書きPR #2](https://github.com/shirai-masatomo/whitespace/pull/2)、branch `codex/dive-dive-playground`。実装 `28ed30e` の DIVE DIVE CI run #174 は **success**。レビュー同期commitは実装品質の前進には数えない。

30分の常設再開 `dive-dive` は承認済み。再承認不要。03:22以降も実装サイクルが再開し、03:50(JST)に `28ed30e` がpushされた。過去の無commit時間の原因が解消したとは断定せず、次回も実装commit時刻を確認する。

環境メモ: sandbox内GodotはAppData設定/証明書にアクセスできないため、この環境では通常権限で検証。GPUラッパーはStart-ProcessのExitCodeと専用ログで判定。呼び出し元の未設定LASTEXITCODEを非0と比較すると後続検査を省略するため、連続呼び出しはStop例外/ログで判定する。

## 次の3タスク

1. DD-064/067: 通常入水84の左泡/右庭に別の「行く理由」を作り、85/86の抜けを保ちながら岩/葉/魚の模型感を大形状と配置から再制作。
2. DD-072: 91の隠れた帰還先、流木再合流3.40%のナイフエッジ、粒子以外の流れ/退避域の手掛かりを改善。速度/O2だけの調整で済ませない。
3. DD-069/067: 70%/80%の危険差を外回りの見返りへ接続し、125〜240mを主役/移動/見返り/O2判断が違う場所として前版比較する。
