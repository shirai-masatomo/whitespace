# DIVE DIVE — PROJECT STATE

更新: 2026-09-20

**Phase 3 / リアル層の場所づくり / CHANGES・SELF_CONTINUE**。人間レビュー待ちではない。最終目標はSteam公開可能なWindowsゲーム。現在はSteamなしでゲームを成立させる段階。

## 現在動くもの

- 海上から入水、自然沈降5m/s、E15m/s・酸素2.5倍、Space6m/s。藻で即補給、酸素切れは同じ世界で救助。450mの既存経路。
- 浅海の藻庭から横へ入り縦穴へ抜ける裂け目。補給地点の先の縁を侵食斜面へ変え、内部を覗ける構図にした。縁の格子状の角を除き、内部の壁に窪みを追加。従来の岩礁歩行・空気洞・屋根/外側・泡上昇流も維持。
- 160〜240mは段丘/西岸の張り出し/下側の藻庭と、侵食された連続岸壁の内外経路。岩柱の反復を層状の欠けへ置換。125mから連続操作で歩行/遊泳できる。
- 張り出しからエイ/魚群を見に沖へ寄り、同じ藻へ戻る試作。満タンで約19.9秒、沖で酸素61.73%、35%開始では救助。生物接近を酸素以外の見返りとして比較中。近景のエイは紙状から曲面へ再制作。
- 通常の開始視点でも水面裏が平らな帯にならず揺らいで見える。左の20m巨大泡と右の藻庭が寄り道先。泡の奥のアーチを浅く奥へ再配置し、穴と支柱を予告。泡→潮流→アーチ→補給は実入力で約29.2秒。
- 最新ビルド出力は `build/windows-agent/DIVE DIVE.exe`。人間の `windows-preview` 等は置換しない。[起動方法](README.md) / [比較と測定](REVIEW_PACKET.md)。

## 検証と同期

最新外部レビュー正本は origin/main `2f7e43f`（`reviewed_through: c87a402`）。作業ブランチの `EXTERNAL_AI_REVIEW.md` へ同内容を同期済み。HUMAN_DIRECTION正本も保持。レビューは、高い根元/低い観察棚/深い切れ込みへの2回目の大形状再制作を前進としつつ、左の大面と観察棚の模型感が残るため人間レビューはまだ不要と判定。前回設定した局所専用mesh A/B条件を満たしたため、DD-070では張り出し1個だけのGodot生成版対Blender/手作りmesh版比較をP0として受領し、DD-071で今回実施した。DD-064（入水右の最初の誘い）、DD-069（125〜160mからの自然発見と入口先の好奇心）、DD-067（通常視線の画面品質）、DD-065（ribbon以外の場所単位）もP0のまま。

張り出しの西根元を高く、酸素の観察棚を低くし、間に泳いで入れる深い切れ込みを追加。下面も根元/沖で別の輪郭へ。旧経路が新しい肩へ衝突したため、上を越える実入力を修正。125mから44.73秒で下側まで到達。切れ込みへ泳ぎ入りSpaceで抜ける4.18秒/残酸素83.27%を追加検査。前版との実画面で高低差は出たが、根元の大面と観察棚の模型感はまだCHANGES。

最終コードで `./dev.ps1 check -BuildFolder windows-agent` 成功。format/lint/依存、ルール479・シーン430・音57・実衝突689・探索30・岩礁/藻庭救助157・入水17・峡谷70、補助の表面2688点、既存11経路、Windows export/EXE headless起動。峡谷Aの両描画確認は前回実施済み。今回は入水のForward+/Compatibility実画面・連続操作と標準visualを確認、配布EXE Forward+120フレームも成功。生物の連続移動線（中心/左右7m）の壁横切り0。標準visualの救助・ゴールも今回再確認済み。入水の魚群表現のみ更新。棄却mesh Bの失敗は通常版から分離。GPUは画面外・無音・非捕捉、自分のプロセスのみ終了。

GitHubは[下書きPR #2](https://github.com/shirai-masatomo/whitespace/pull/2)、作業ブランチは `codex/dive-dive-playground`。峡谷版 `8fc0448` のCI（run99）は成功。生物の別入口版 `733cdd6` のCI（run104）も成功。直前版e7ffe4cのCI run108成功。張り出し/湾版30662dfのCI run117成功。浅海斜面版42467a2のCI run119成功。切れ込み版2701ab8のCI run129成功。今回の魚群版はpush後の対象SHAを確認する。ユーザーの継続的な明示許可を受け、30分ごとの自律再開設定 `dive-dive` をACTIVEで作成済み。同じ範囲の再承認は不要。許可範囲・除外事項はAUTONOMYに記録。ゲームの判定はCHANGESのまま。

最新試作: 張り出し1個の専用meshを粗形状/細分化の2案で実GPU比較。大面が残り、鋭い三角面/下面の縮みで悪化したため不採用。通常ゲームは生成版Aを維持。比較用の再現ツールだけを残し、候補meshはartifactsに隔離。続いて入水の魚群を藻庭へ向きながら横切る群れへ変更し、画面端で切れる初案を内側へ移動。左の泡と右の生物で性質が違う手掛かりになったが、製品品質は未合格。

## 未解決と次の作業

1. DD-070 / DD-067: DD-071の局所mesh A/Bは今回実施してBを棄却。通常版Aの大面は依然未合格。方式変更そのものを改善扱いせず、次は峡谷の帯状骨格を1区間の場所へ変える。
2. DD-064: 通常入水で右側へ向く魚群を比較し、画面端に切れる初案を修正。魚の反復形/折り返しと到着後の未知は継続課題。「右へ寄る理由→補給後に裂け目発見」の二段階を通常視点で作る。
3. DD-065 / DD-069: 長いribbon骨格の少なくとも1区間を独立した場所単位へ置換する。生物→沖→下側入口は実到達可能なので、125〜160mの通常経路から自然に発見できるかを確認し、入口先に深部予告/観察点/近道など次の好奇心を作る。

再開の実態: 18:10の42467a2以降、18:44の自動再開は18:45頃に途切れ、21:34の人間の状況確認まで開発差分なし。設定はACTIVE。21:33のアプリ起動後にツール結果欠落/通信再試行を確認したが、停止原因は未特定。承認/テスト待ちではない。22:23に常設再開を受信し開発・検証を実行。1回の再開成功と、前回の停止原因の解消は区別する。

環境メモ: sandbox内のGodot importはAppData設定/証明書にアクセスできず失敗する。この環境では通常権限でheadless checkを実行する。

旧レビューPNG70枚と孤立した旧東側アーチを整理。最新証跡・再現コード・履歴・使用中ビルドは保持。別GPU/初見探索/長時間プレイは未確認。保存/Steam SDK/450m以深は未実装。
