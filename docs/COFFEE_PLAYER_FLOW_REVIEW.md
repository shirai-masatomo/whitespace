# プレイヤー視点の図・場面選択の作業記録（2026-09-10）

## 基準と判断

作業場所は `C:\Users\masat\.codex\worktrees\2f05\codex_test`。最新リモートを取得し、codex/coffee-scene-presence のccb005dと差0/0・変更なしを確認。codex/coffee-player-flowを作成。AGENTS、設計思想書、仕様・状態文書を参照。サブエージェントは使用していない。

設計思想書の「内部で受諾・実行を区別すること」と「どの行動を省略して表現するかはゲーム設計で決める」を踏まえ、図の入口をプレイヤーの操作順へ変更した。改善候補は現行図と分け、更新規則は変更しない。

## 図

[入口](COFFEE_PLAYER_FLOW.md) → A/B/Cの初手一覧 → 初手ごとの詳細27ページ。各詳細は初手の実反応、2手目の全9候補とその実反応、続行時の全候補と引き継ぐ内容を持つ。ループは同じ行為をさらに1回実行して、発話・実動作・全更新状態が同じときだけ描く。別履歴の枝は合流しない。受取から拭く共通例のみ3手目を展開し、それ以外の3手目以降の全返答は未展開と明記。

27初手・243通りの2手の組合せを、実装を呼んで生成した。モデル出力や個人のプレイログを図にしたものではない。旧内部図と全状態表は変更せず保持。

## UI

「あなたは、この場にどう居合わせた？」という問いと、関係・事故への関与が分かる3つのラジオ選択をタイトル直下に配置。選択中の背景とラジオの両方で現在位置を示す。導入文は観察と立場だけに限定し、隠れた希望は説明しない。枠を増やさず、初期化の注意は小さく一文にした。

幅390pxでは縦3列、広い画面では横並び。実ブラウザでラジオが共通inputの高さを継承して大きくなったため、input自体の最小高さを解除し、選択の領域は40pxに調整した。以前の検証欄から選択部品を撤去し、内部識別子と全操作確認は残した。

## 実施した確認

- 52テスト、lint、build成功。最後のCSS調整後もbuild成功。既存Three.jsの約537kB警告は継続。
- coffeeScene.js、coffeeRules.js、coffeeLanguage.jsのccb005dとの差分なし。判定・状態更新・モデル契約は変更していない。
- ブラウザでA/B/Cの導入と選択表示を確認。Aで質問→B切替、Bで援助辞退→C切替、Cで謝罪→A切替を操作。Cの初回謝罪の反応を確認。
- Aで提案→紙を渡す→B切替。履歴0件、濡れた机、紙は主人公の手元、渡す操作が有効に戻ることを確認。
- 1280×900と390×844で確認。狭い画面はclientWidth/scrollWidthとも375pxで横はみ出しなし。Bでも通常の援助提案を操作できた。

## 未確認・次の議論

全243枝をブラウザで一つずつ実行したわけではない。生成処理での実行とUI実操作は区別する。3手目以降の全返答の展開、モデルの再測定、実IME、実スマートフォンは今回未実施。実装の既存状態遷移テストは継続して成功している。

まず受取後の「拭くのを見守る」を自動化する案を議論したい。受取と拭く内部イベントは分けたまま、本人が自然に続けて動ける。受諾と受け渡しの一体化は、プレイヤーが引き下がる余地を削るため別に検討する。今回これらは未実装。

## GitHub表示・保存の最終確認

UIをd7fb99a、図と文書を2aabcd1へコミットし、codex/coffee-player-flowへpushした。GitHubの入口→A/B/C初手一覧、Aのティッシュ提案・Bの援助提案・Cの謝罪の詳細、共通続きの7ページを開き、58図の描画をブラウザの文書構造で確認。代表の初手・受け渡し・拒否後・再謝罪ループ・拭き取りは実画面で文字と矢印を確認した。残り24詳細ページ（240図）のGitHub上の個別描画は未確認。全ページは同じ生成処理を使い、ローカルでは32ファイル・298図・相対リンクの参照先欠落0を確認した。

再生成前後のハッシュは一致。git diff --check成功。画面幅の一時指定は解除した。mainへのマージ・アプリ公開は行っていない。
