# 現在地と次の作業

最終整理：2026-09-10。現行アプリは `prototypes/001-nadameyo`。起動は [README](../README.md)、主眼は [設計思想](DESIGN_PHILOSOPHY.md)。この文書を、現在できること・未解決事項・直近の検証の正本にします。

## 現在できること

| 場所 | できること |
| --- | --- |
| コーヒー場面 | 後から来た友人／近くの他人／ぶつかった友人を選ぶ。同じ謝罪・援助でも反応が変わる。発話と動作、知識・同意・物の移動を分け、机と胸像で状態を表示。切替・再開始で初期化 |
| 宥めよ | 未分類・保留は数値を変えず1発言消費。同文反復は再加点せず、強い言葉の直後の新しい謝罪で回復。成功・失敗・再挑戦・振り返り |
| 胸像 | 信頼は向きと光、緊張は肩と動きへ独立反映。動き軽減、3D失敗時の代替表示、ゲームを書き換えない開発パネル |
| 言語比較 | レビュー3例＋生成25例を辞書と小型LLMで比較。形式不正・接続失敗を成功結果と分ける。ゲームの標準判定は辞書のまま |

コーヒー場面に得点・5ターン制・勝敗はありません。質問しなくても援助を提案できます。自由入力は解釈を確認してから適用する実験機能で、選択肢の代替としての精度には達していません。具体的な流れは [分岐図](COFFEE_FLOW.md)。

## 次に議論・確認すること

1. 受取後の「拭くのを見守る」を省き、相手が自動で拭く版を比較する。内部の受取・拭き取りは分けたままでよい。今回は未実装。
2. 初見の人が、説明より先に紙の移動・片付け・距離・姿勢を読み取れるか観察する。
3. 関係と希望を一つずつ変える。現在のA/Bは両方が違うので、どちらの効果か独立に測れていない。
4. 自由入力は、行為の意味と実行可否を混同しないか、未使用の言い換えでも試す。実測と失敗例は [モデル資料](LOCAL_MODEL.md)。

後回しの検討：同義反復・否定範囲・主語・皮肉・長文、呼称/距離の希望/許し、43件の保留語、empathy/accountability/offering_space等の分類、履歴保存。性格システム・新モード・他場面への汎用化は未着手で、必要性を確かめてから扱います。言語ジェンガと悩み由来の対話モードは[将来のアイディア](FUTURE_IDEAS.md)に分離した検討候補であり、現在の実装要件ではありません。

## 確認済みと限界

- 2026-09-05〜10：辞書修正、ゲーム成功・失敗・反復・回復・再挑戦、比較ラボの実モデル/停止/中止/再接続、コーヒーA/B/Cの主要経路をブラウザで確認。
- 2026-09-08：胸像の信頼/緊張4組合せ、表示プレビュー、WebGLの強制接続喪失→代替→復帰を確認。ラボ往復でcanvasと描画インスタンス1→0、共有資源17個の解放を確認。実GPU故障の検証ではない。
- 2026-09-10：未開示の希望・架空の拒否履歴・片付け後の初質問を回帰テスト化。52テストで全1,403到達状態・12,627遷移の不変条件も検証。全台詞の自然さを保証するものではない。
- 同日：場面選択をタイトル下へ移動。導入A/B/C、切替後の会話0件・濡れた机・紙の所持を確認。1280×900と390×844（内容375px、横はみ出しなし）、productionでの開発パネル非表示を過去の対応変更時に確認。
- 実IME変換確定Enter、実スマートフォン、OSの動き軽減の実切替、低性能端末や長時間GPU計測、JSON保存のOS上の保存先確認は未実施。ブラウザ文字入力や幅指定を実機確認とは扱わない。
- Three.jsの約537kB（gzip約135kB）チャンク警告が残る。外部辞書の参照版・必要なライセンス表示は配布準備時に再確認する。ルートの旧index.html/app.js/style.cssは現行アプリで使わず、整理は別途判断する。

## 今回の文書整理

基準92b18bb・最新リモート差0/0・未コミットなしから `codex/readable-docs` へ分岐。アプリの挙動は変更しません。

- 31個の分岐ページと巨大表は、人が読む代表4図と必要時だけの機械出力へ置換。
- PROJECT_STATE/TODO/ACCEPTANCE/APP_STATEと個別レビューの現在情報をここへ、仕様・開発手順をDEVELOPMENTへ、モデルの固有実測をLOCAL_MODELへ統合。旧文書と重複したPLAYTEST/COMMAND_LOGは削除。古い変更の逐次経緯はGit履歴で辿れる。
- READMEを全体の入口と起動手順の正本にし、PROJECT_MAPを削除。アプリ側READMEは参照だけにする。
- 辞書JSON・144件の採否理由/出典/利用条件・実測JSONは保持。別環境でのレビューはassistantへの委任であり、ユーザー本人が全件承認したという意味ではない。

小さな変更ごとの報告書は増やさず、この現在地と影響する仕様だけを更新します。実行コマンド・検証結果は直近の作業分を短く残し、恒久的な手順は開発資料へ反映します。

直近の確認（文書整理）：Mermaid CLI 11.17.0で4図を画像化し、分岐と各結果の線を目視。Bの再提案の線で列が崩れたため、拒否後の再提案を別の箱にして3選択を比較できるよう修正。全状態の一時出力、図の一時抽出、辞書一覧の再生成を実行。`node scripts/check-doc-links.mjs` は9文書・リンク切れ0、`npm test` は52件成功、`npm run lint` 成功、`git diff --check` 成功。アプリsrc・依存・実測JSONは92b18bbと同一のため、今回buildとゲームの再操作は実施していない。Markdownは56→9ファイル、読み物の図は298→4。GitHubでも4図すべての描画を確認。代表図は縦に長かったため2回目の反応までに絞り、画像で3分岐と結果を同時に比較できることを再確認した。受取後の拭き取りは直下の説明に残した。

アイディア整理：言語ジェンガの本文をFUTURE_IDEASへ移し、悩み由来の場面・他者の対話履歴を提供者が振り返る案を追加。設計思想とREADMEから参照。アプリの変更なし。リンク確認・差分チェックを実施。
